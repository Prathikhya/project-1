<style>
  .search-box{
    border: 1px solid black;
    border-radius: 10rem;
    padding: 0 2rem;
  }
  .btn{
    border-radius: 10rem ;
  }
</style>

<nav class="navbar navbar-expand-lg bg-body-tertiary sticky-top">
    <div class="container-fluid">
      <!-- left side -->
      <a class="navbar-brand" href="http://localhost:8080/listings">PartIT</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link" aria-current="page" href="http://localhost:8080/listings">Explore</a>
      </div>

      <!-- middle side -->
      <div class="navbar-nav ms-auto">
        <form class="d-flex" role="search">
          <input class="form-control me-2 search-box" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-success searchbtn" type="submit">Search</button>
        </form>
      </div>

          <!-- right side -->
          <div class="navbar-nav ms-auto">
            <a class="nav-link" href="http://localhost:8080/listings/new"><i class="fa-solid fa-plus"></i></a>
            <% if(!currentUser) { %>
          <a class="nav-link" href="/signin"><b>SignIn</b></a>
          <a class="nav-link" href="/login"><b> Login </b></a>
          <% } %>
          <% if(currentUser) { %>
          <a class="nav-link" href="/logout"> <b>Logout</b> </a>
          <% } %>
          </div>
      </div>
      </div>
    </div>
  </nav>